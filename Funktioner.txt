/*
Struct Proces
- Indeholder Prioritet, PID, Burst time

Comparision funktion
- Lav prio > Høj Prio
    -> Generelt Højere Burst time = Lavere prio

Apply PrioQueue algortime

Find ventetid + Total tid brugt
    - Find gennemsnitlig ventetid
    - Find gennemsnitlig total tid brugt

Udnyt RAII locking

Tilføj process states

Class-based design

Tilføj Round-Robin Scheduler

Tilføj 'simuleret tid'

Kombiner RR + Prio -> Hver Prioritet har en Queue
- RR indenfor hvert prioritets-niveau

Vedtag nogle preemption regler
- Hvis ny høj-prioritets proces kommer ind, overtager den?
    - CFS, Re-evaluate prio queue efter hver quantum

Indfør Aging 
- base_priority (original prio)
- prio (current prio)
- waiting_time

Logging subsystem -> Enten CSV eller JSON 
- Udskriv detaljer af hver process til log

Simuler I/O v CPU bound processer 
- bool io bound
- int io interval
- int cpu used
    -> Husk I/O er blokerende
        -> Hvis ratio er høj, kan man justere automatisk.

<-------- FÆRDIG HER! -------->

Checklist
Area	            Change	                        Benefit
Architecture	    Split Scheduler/IO/Logger	    Cleaner design
Logging	            Batch JSON writes	            Faster & cleaner  // FÆRDIG flusher logs til sidst!
Queue Ops	        Use deque, clamp priorities	    Safety & performance //FÆRDIG -> Skriv egen deque? 
Output	            Add verbosity flag	            Manageable logs  // FÆRDIG bruges nu debug!

Architecture:
Split scheduler op i: 
    - Process State
    - IO Queues
    - Time Advancement 
    - Logger

Gøres ved brug af: 
    Scheduler: Håndtere orkestreringen og schedule beslutninger
    Process Control Block (PCB): Håndtere en process interne status (CPU brug, IO vente og lign.)
    IOManager: Håndtere IO vente og logik 
    Logger: Håndtere logning af processerne. (Allerede implementeret)






Gantt Chart
- Kan visualisere CPU optagethed

Dynamisk Process ankomst
- Simuler at nye processer ankommer mid-run

Per-prioritets statistik
- Avg ventetid, turnaround -> hjælper ift. CFS

Fix vente-tid + total time, når der bruges RR


*/


